# Optional. For local YouTube processing, point the browser to the local resolver:
# VITE_VIDEO_PROXY_URL=http://localhost:8787/api/video
#
# Cloudflare Pages uses the same-origin /api/video automatically, so no VITE
# variable is required when the Pages Function is deployed with the site.
